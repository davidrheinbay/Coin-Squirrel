<div class="container">
  <div class="row">
    <div class="cards">
      <div class="col-md-3">
        <% @partners.each do |partner| %>
          <%= render 'components/partner_card', partner: partner %>
        <% end %>
      </div>
    </div>
  </div>
</div>

